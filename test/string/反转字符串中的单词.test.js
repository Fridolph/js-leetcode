import revertByWord from '../../code/string/反转字符串中的单词'

test(`revertByWord: Let's take LeetCode contest`, () => {
  expect(revertByWord(`Let's take LeetCode contest`)).toBe(`s'teL ekat edoCteeL tsetnoc`)
})
